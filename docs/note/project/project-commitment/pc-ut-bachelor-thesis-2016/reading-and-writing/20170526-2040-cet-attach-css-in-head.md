* Basic CSS setup in `<head>`.
* Use `../` to up one level relative to the HTML file.

```markdown
<link rel="stylesheet" href="../styles.css">
<link rel="stylesheet" href="html/styles.css">
<link rel="stylesheet" href="styles.css">
```">
```